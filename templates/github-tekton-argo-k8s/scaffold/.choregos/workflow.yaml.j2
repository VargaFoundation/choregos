# Workflow de ce projet. `choregos workflow validate .choregos/workflow.yaml` avant de committer.
# Repris du template `{{ workflow_template | default("default-simple") }}` : adaptez les libellés
# d'état à votre board, gardez les gates.
{{ workflow_yaml }}
