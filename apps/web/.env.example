# API Choregos (relayée par Next en développement)
CHOREGOS_API_URL=http://localhost:8000
# `mock` sert des fixtures locales : le front avance sans l'API
NEXT_PUBLIC_API_MODE=live
NEXT_PUBLIC_DEFAULT_ORG=varga
